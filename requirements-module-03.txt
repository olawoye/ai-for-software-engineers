streamlit>=1.28.0
httpx>=0.24.0
pandas>=2.0.0
openai>=1.3.0

# Core dependencies for API integration
# Note: OpenRouter supports 100+ models via unified API
# No model-specific packages needed; all requests go through OpenRouter endpoint
