# Project Completed

This folder contains reference implementations for the course lessons. Treat these files as read-only examples that Codex/Claude agents can point learners to while they build the TODO scaffolds.

Each module has its own directory so you can flip through working code per lesson.
